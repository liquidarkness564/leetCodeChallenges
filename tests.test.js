const functions = require('./linkedListAddTwoNumbers/linkedListAddTwoNumbers.js');

describe('Linked List Add Two Numbers', () => {
  it('should be a function', () => {
    expect(functions.addTwoNumbers).toBe(function);
  })
})